import { ComingSoon } from "@/components/ComingSoon";

export default function SourcesPage() {
  return (
    <ComingSoon
      title="Sources"
      blurb="Outlet profiles with aggregated tendency markers on the bias spectrum, computed after publication from per-article predictions — never assigned by hand."
    />
  );
}
