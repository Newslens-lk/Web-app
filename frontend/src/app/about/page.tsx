import { ComingSoon } from "@/components/ComingSoon";

export default function AboutPage() {
  return (
    <ComingSoon
      title="About NewsLens"
      blurb="How events are grouped, how bias is predicted per-article and aggregated per-outlet, and the limitations of the current model — full copy lands with the other views."
    />
  );
}
