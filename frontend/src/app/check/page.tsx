import { ComingSoon } from "@/components/ComingSoon";

export default function CheckPage() {
  return (
    <ComingSoon
      title="Check an Article"
      blurb="On-demand bias check — paste a Sinhala article or a URL and get a prediction with a confidence score and the nearest related event cluster."
    />
  );
}
